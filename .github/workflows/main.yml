name: PR Workflow

on:
  workflow_dispatch:
    inputs:
      source:
        type: choice
        description: Source branch
        required: true
        options: 
        - dev
        - qa
      dest:
        type: choice
        description: Dest branch
        required: true
        options: 
        - dev
        - qa

jobs:
 createPullRequest:
    runs-on: ubuntu-latest
    steps:

      - name: Make changes to pull request
        run: date +%s > hello

      - name: Create Pull Request
        id: cpr
        uses: naomilev/PRWorkflow@v5
        with:
          branch: ${{ github.event.inputs.source }}
          base: ${{ github.event.inputs.dest }}
          title: '[Example] Update report'
          body: |
            Update report
            - Updated with *today's* date
            - Auto-generated by [create-pull-request][1]

             
